# Plugin Configuration

<!-- Controls which plugins are active. Plugin names match directory names in plugins/.
     Copy this file to data/plugin-activation.md to customize activation.
     If data/plugin-activation.md does not exist, all plugins are active by default. -->

mode: all

<!-- Options:
  - all         — every plugin in plugins/ is active (default)
  - blocklist   — all active EXCEPT those listed under ## Disabled
  - allowlist   — ONLY those listed under ## Enabled are active
-->

## Disabled
<!-- List plugin directory names to deactivate (blocklist mode only):
- fintech
- academic-cv
-->

## Enabled
<!-- List plugin directory names to activate (allowlist mode only):
- fintech
-->
