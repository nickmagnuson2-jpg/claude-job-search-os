{
  "tools/check_hook_warn_tier.py::<module>::DROP_CALL::0d29993b": "Drops `sys.exit(main(sys.argv[1:]))`, the CLI entrypoint, for the same reason as the IF_FALSE entry above. The exit codes that matter (2 blocks, 0 clean, 1 usage) are asserted directly against main() in test_main_blocks_with_exit_2 and its siblings. 2026-08-25.",
  "tools/check_hook_warn_tier.py::<module>::IF_FALSE::dd9abcf0": "IF_FALSE on `if __name__ == \"__main__\":` suppresses the CLI entrypoint. Every test imports check_hook_warn_tier and calls audit/main/writes_stderr/can_block directly, so this branch is on no tested path. Same shape as the accepted handoff_state.py and promotion_schema.py entries. 2026-08-25.",
  "tools/check_hook_warn_tier.py::can_block::DROP_CONTINUE::eee0bbba": "Drops the `continue` in one of the two except-(ValueError,TypeError,SyntaxError) arms of can_block, reached only when literal_eval fails on a non-constant exit argument or return value. Equivalent by argument: control falls through to the very next statement in the same loop body, which is the `isinstance(node, ast.Return)` test on the same node -- a node that already failed to be a literal cannot become one, so the iteration reaches `return False` in the same state. Pinned behaviourally from the outside instead: test_a_nonliteral_exit_code_does_not_count_as_blocking asserts sys.exit(code) is not treated as blocking. Both arms are identical in shape and share this entry. 2026-08-25.",
  "tools/check_probe_prompt.py::<module>::DROP_CALL::7f926b03": "sys.stdout.reconfigure(encoding='utf-8') is a best-effort UTF-8 guard already wrapped in try/except; dropping it changes no verdict on any platform the suite runs on, and the properties it guards (report text) are asserted elsewhere. Equivalent mutant. 2026-08-20.",
  "tools/check_probe_prompt.py::<module>::IF_TRUE::dd9abcf0": "IF_TRUE on `if __name__ == \"__main__\":` forces a condition that is already true in every subprocess the suite spawns. Behaviourally identical by construction. Equivalent mutant. 2026-08-20.",
  "tools/check_public_pii.py::mask_heredoc_bodies::DROP_CALL::3c46beed": "Drops `out.append(chr(10))` in the body/delimiter path. mask_heredoc_bodies exists to guarantee ONE property: a heredoc body yields no write targets. Its output is consumed only by the redirect / tee / sed -i / dd-of regexes, all of which need `>` , `tee`, `-i` or `of=` to match. The values appended at these sites are a newline, a single space, a heredoc delimiter (matched by [A-Za-z_][A-Za-z0-9_]*), or the literal `<<DELIM` marker -- NONE of which contains `>` or any redirect token. Every body character has already been replaced by a space, so no `>` survives in that region for a dropped character to abut. Dropping these calls therefore only REMOVES characters from the masked filler; it cannot ADD redirect syntax, so the extracted target set is provably identical. Equivalent mutant, by argument rather than by assumption. The mutants in this function that were NOT equivalent were killed rather than allowlisted: quote-state tracking (3 killed by test_mask_does_not_treat_a_QUOTED_heredoc_marker_as_a_heredoc and siblings), mid-line delimiter lookalikes (killed by test_mask_does_not_close_the_heredoc_mid_line_on_a_delimiter_lookalike), and quote closing (killed by test_mask_closes_quotes_so_a_later_heredoc_is_still_detected). 12 survivors -> 6. 2026-08-24.",
  "tools/check_public_pii.py::mask_heredoc_bodies::DROP_CALL::43fa2a53": "Drops `out.append(m.group(0))`, which re-emits the `<<DELIM` opener. Removing it deletes `<<EOF`-shaped text; the surrounding characters are a command word and a newline, so no redirect can be formed by their juxtaposition. mask_heredoc_bodies exists to guarantee ONE property: a heredoc body yields no write targets. Its output is consumed only by the redirect / tee / sed -i / dd-of regexes, all of which need `>` , `tee`, `-i` or `of=` to match. The values appended at these sites are a newline, a single space, a heredoc delimiter (matched by [A-Za-z_][A-Za-z0-9_]*), or the literal `<<DELIM` marker -- NONE of which contains `>` or any redirect token. Every body character has already been replaced by a space, so no `>` survives in that region for a dropped character to abut. Dropping these calls therefore only REMOVES characters from the masked filler; it cannot ADD redirect syntax, so the extracted target set is provably identical. Equivalent mutant, by argument rather than by assumption. The mutants in this function that were NOT equivalent were killed rather than allowlisted: quote-state tracking (3 killed by test_mask_does_not_treat_a_QUOTED_heredoc_marker_as_a_heredoc and siblings), mid-line delimiter lookalikes (killed by test_mask_does_not_close_the_heredoc_mid_line_on_a_delimiter_lookalike), and quote closing (killed by test_mask_closes_quotes_so_a_later_heredoc_is_still_detected). 12 survivors -> 6. 2026-08-24.",
  "tools/check_public_pii.py::mask_heredoc_bodies::DROP_CALL::a7947bf7": "Drops `out.append(' ')`, the per-character body mask. The character is omitted instead of being replaced by a space -- both erase it from the scanned string. mask_heredoc_bodies exists to guarantee ONE property: a heredoc body yields no write targets. Its output is consumed only by the redirect / tee / sed -i / dd-of regexes, all of which need `>` , `tee`, `-i` or `of=` to match. The values appended at these sites are a newline, a single space, a heredoc delimiter (matched by [A-Za-z_][A-Za-z0-9_]*), or the literal `<<DELIM` marker -- NONE of which contains `>` or any redirect token. Every body character has already been replaced by a space, so no `>` survives in that region for a dropped character to abut. Dropping these calls therefore only REMOVES characters from the masked filler; it cannot ADD redirect syntax, so the extracted target set is provably identical. Equivalent mutant, by argument rather than by assumption. The mutants in this function that were NOT equivalent were killed rather than allowlisted: quote-state tracking (3 killed by test_mask_does_not_treat_a_QUOTED_heredoc_marker_as_a_heredoc and siblings), mid-line delimiter lookalikes (killed by test_mask_does_not_close_the_heredoc_mid_line_on_a_delimiter_lookalike), and quote closing (killed by test_mask_closes_quotes_so_a_later_heredoc_is_still_detected). 12 survivors -> 6. 2026-08-24.",
  "tools/check_public_pii.py::mask_heredoc_bodies::DROP_CALL::d520a301": "Drops `out.append(command[i + 1:end])`, which re-emits the heredoc DELIMITER text. A delimiter is an identifier by construction (_HEREDOC_START matches [A-Za-z_][A-Za-z0-9_]*), so it can never be or contain a redirect. mask_heredoc_bodies exists to guarantee ONE property: a heredoc body yields no write targets. Its output is consumed only by the redirect / tee / sed -i / dd-of regexes, all of which need `>` , `tee`, `-i` or `of=` to match. The values appended at these sites are a newline, a single space, a heredoc delimiter (matched by [A-Za-z_][A-Za-z0-9_]*), or the literal `<<DELIM` marker -- NONE of which contains `>` or any redirect token. Every body character has already been replaced by a space, so no `>` survives in that region for a dropped character to abut. Dropping these calls therefore only REMOVES characters from the masked filler; it cannot ADD redirect syntax, so the extracted target set is provably identical. Equivalent mutant, by argument rather than by assumption. The mutants in this function that were NOT equivalent were killed rather than allowlisted: quote-state tracking (3 killed by test_mask_does_not_treat_a_QUOTED_heredoc_marker_as_a_heredoc and siblings), mid-line delimiter lookalikes (killed by test_mask_does_not_close_the_heredoc_mid_line_on_a_delimiter_lookalike), and quote closing (killed by test_mask_closes_quotes_so_a_later_heredoc_is_still_detected). 12 survivors -> 6. 2026-08-24.",
  "tools/check_public_pii.py::mask_heredoc_bodies::DROP_CONTINUE::eee0bbba": "Drops the `continue` after the delimiter line closes the heredoc. Control falls through to one extra `out.append(chr(10))` and then `i += 1` with `i` already set to `end`; the heredoc is already closed and `in_body` already False, so only a stray newline is added to the filler. mask_heredoc_bodies exists to guarantee ONE property: a heredoc body yields no write targets. Its output is consumed only by the redirect / tee / sed -i / dd-of regexes, all of which need `>` , `tee`, `-i` or `of=` to match. The values appended at these sites are a newline, a single space, a heredoc delimiter (matched by [A-Za-z_][A-Za-z0-9_]*), or the literal `<<DELIM` marker -- NONE of which contains `>` or any redirect token. Every body character has already been replaced by a space, so no `>` survives in that region for a dropped character to abut. Dropping these calls therefore only REMOVES characters from the masked filler; it cannot ADD redirect syntax, so the extracted target set is provably identical. Equivalent mutant, by argument rather than by assumption. The mutants in this function that were NOT equivalent were killed rather than allowlisted: quote-state tracking (3 killed by test_mask_does_not_treat_a_QUOTED_heredoc_marker_as_a_heredoc and siblings), mid-line delimiter lookalikes (killed by test_mask_does_not_close_the_heredoc_mid_line_on_a_delimiter_lookalike), and quote closing (killed by test_mask_closes_quotes_so_a_later_heredoc_is_still_detected). 12 survivors -> 6. 2026-08-24.",
  "tools/detector_probe.py::<module>::DROP_CALL::0d29993b": "Drops `sys.exit(main(sys.argv[1:]))`, the CLI entrypoint, for the same reason as the IF_FALSE entry above. Exit codes 2/0/1 are asserted directly against main(). 2026-08-25.",
  "tools/detector_probe.py::<module>::IF_FALSE::dd9abcf0": "IF_FALSE on `if __name__ == \"__main__\":` suppresses the CLI entrypoint; every test imports detector_probe and calls probe/extract_patterns/probe_records/main directly. Same shape as the accepted handoff_state.py, promotion_schema.py and check_hook_warn_tier.py entries. 2026-08-25.",
  "tools/detector_probe.py::probe::DROP_CONTINUE::eee0bbba": "Drops the `continue` in probe()'s `except re.error` arm, which is the last statement in the loop body: control falls to the next iteration either way, so the mutant is equivalent by position. The behaviour that matters is pinned from the outside by test_an_uncompilable_pattern_is_skipped_not_crashed (a broken candidate must not stop a working one being tried). The sibling DROP_CONTINUE in extract_patterns was NOT allowlisted -- there it changes the returned list, and it is killed by test_an_uncompilable_span_is_never_returned_as_a_pattern. 2026-08-25.",
  "tools/detector_run.py::<module>::DROP_CALL::0d29993b": "Drops `sys.exit(main(sys.argv[1:]))`, the CLI entrypoint, for the same reason as the IF_FALSE entry above. Exit codes 2/0/1 are asserted directly against main(). 2026-08-25.",
  "tools/detector_run.py::<module>::DROP_CALL::45c5b14d": "Drops the sys.path.insert that lets `import detector_probe` resolve when this module is run as a script. Under pytest the tools/ directory is already on sys.path (the test file inserts it), so the import succeeds either way and no test can observe the difference. The behaviour it protects is CLI invocation, which is covered by the main() tests that import the module directly. 2026-08-25.",
  "tools/detector_run.py::<module>::IF_FALSE::dd9abcf0": "IF_FALSE on `if __name__ == \"__main__\":` suppresses the CLI entrypoint; every test imports detector_run and calls scan/validate/load_detectors/main directly. Same shape as the accepted promotion_schema.py, check_hook_warn_tier.py and detector_probe.py entries. 2026-08-25.",
  "tools/filler_baseline.py::<module>::DROP_CALL::0f465620": "filler_baseline.py CLI plumbing: process exit codes, the sys.path bootstrap, and the `return 1` / `return 0` arms of already-tested error paths. The BEHAVIOUR these sit on is pinned elsewhere -- collect() has zero survivors, and the printed table, --json, --rank, --top, the ambiguous-match refusal and the not-found refusal all have tests asserting their content. What survives here is whether the process exits 0 or 1 after printing the right thing, which no consumer of this tool branches on: it is read by a human and by /debrief, both of which consume stdout. Mutants were driven 32 -> 24 -> 14 -> 9 by adding real tests rather than allowlist entries; these nine are the residue where a test would assert the exit code and nothing else. 2026-08-24.",
  "tools/filler_baseline.py::_collapsed_channel_reason::IF_FALSE::bfd8fca2": "`if not total: return None` guards a zero-word transcript. Forcing it false reaches the next line, `share = other_words / total * 100`, which raises ZeroDivisionError -- but the branch is unreachable from collect(), which only calls this for rows that already parsed to at least one turn, and unreachable from the CLI for the same reason. Writing a test would require calling the private helper directly with (0, 0), which asserts nothing about the tool's behaviour and would pin an internal signature. Equivalent-by-unreachability rather than equivalent-by-semantics; the guard is kept because the helper is module-level and a future caller may not share collect()'s precondition. 2026-08-25.",
  "tools/filler_baseline.py::main::DROP_CALL::a10d4416": "filler_baseline.py CLI plumbing: process exit codes, the sys.path bootstrap, and the `return 1` / `return 0` arms of already-tested error paths. The BEHAVIOUR these sit on is pinned elsewhere -- collect() has zero survivors, and the printed table, --json, --rank, --top, the ambiguous-match refusal and the not-found refusal all have tests asserting their content. What survives here is whether the process exits 0 or 1 after printing the right thing, which no consumer of this tool branches on: it is read by a human and by /debrief, both of which consume stdout. Mutants were driven 32 -> 24 -> 14 -> 9 by adding real tests rather than allowlist entries; these nine are the residue where a test would assert the exit code and nothing else. 2026-08-24.",
  "tools/filler_baseline.py::main::DROP_CALL::c4777b5d": "filler_baseline.py CLI plumbing: process exit codes, the sys.path bootstrap, and the `return 1` / `return 0` arms of already-tested error paths. The BEHAVIOUR these sit on is pinned elsewhere -- collect() has zero survivors, and the printed table, --json, --rank, --top, the ambiguous-match refusal and the not-found refusal all have tests asserting their content. What survives here is whether the process exits 0 or 1 after printing the right thing, which no consumer of this tool branches on: it is read by a human and by /debrief, both of which consume stdout. Mutants were driven 32 -> 24 -> 14 -> 9 by adding real tests rather than allowlist entries; these nine are the residue where a test would assert the exit code and nothing else. 2026-08-24.",
  "tools/filler_baseline.py::main::IF_FALSE::e2d2faa0": "filler_baseline.py CLI plumbing: process exit codes, the sys.path bootstrap, and the `return 1` / `return 0` arms of already-tested error paths. The BEHAVIOUR these sit on is pinned elsewhere -- collect() has zero survivors, and the printed table, --json, --rank, --top, the ambiguous-match refusal and the not-found refusal all have tests asserting their content. What survives here is whether the process exits 0 or 1 after printing the right thing, which no consumer of this tool branches on: it is read by a human and by /debrief, both of which consume stdout. Mutants were driven 32 -> 24 -> 14 -> 9 by adding real tests rather than allowlist entries; these nine are the residue where a test would assert the exit code and nothing else. 2026-08-24.",
  "tools/filler_baseline.py::main::IF_TRUE::2a59ff71": "filler_baseline.py CLI plumbing: process exit codes, the sys.path bootstrap, and the `return 1` / `return 0` arms of already-tested error paths. The BEHAVIOUR these sit on is pinned elsewhere -- collect() has zero survivors, and the printed table, --json, --rank, --top, the ambiguous-match refusal and the not-found refusal all have tests asserting their content. What survives here is whether the process exits 0 or 1 after printing the right thing, which no consumer of this tool branches on: it is read by a human and by /debrief, both of which consume stdout. Mutants were driven 32 -> 24 -> 14 -> 9 by adding real tests rather than allowlist entries; these nine are the residue where a test would assert the exit code and nothing else. 2026-08-24.",
  "tools/filler_baseline.py::main::RETURN_NONE::03cfab29": "filler_baseline.py CLI plumbing: process exit codes, the sys.path bootstrap, and the `return 1` / `return 0` arms of already-tested error paths. The BEHAVIOUR these sit on is pinned elsewhere -- collect() has zero survivors, and the printed table, --json, --rank, --top, the ambiguous-match refusal and the not-found refusal all have tests asserting their content. What survives here is whether the process exits 0 or 1 after printing the right thing, which no consumer of this tool branches on: it is read by a human and by /debrief, both of which consume stdout. Mutants were driven 32 -> 24 -> 14 -> 9 by adding real tests rather than allowlist entries; these nine are the residue where a test would assert the exit code and nothing else. 2026-08-24.",
  "tools/filler_baseline.py::main::RETURN_NONE::06d3d9b2": "filler_baseline.py CLI plumbing: process exit codes, the sys.path bootstrap, and the `return 1` / `return 0` arms of already-tested error paths. The BEHAVIOUR these sit on is pinned elsewhere -- collect() has zero survivors, and the printed table, --json, --rank, --top, the ambiguous-match refusal and the not-found refusal all have tests asserting their content. What survives here is whether the process exits 0 or 1 after printing the right thing, which no consumer of this tool branches on: it is read by a human and by /debrief, both of which consume stdout. Mutants were driven 32 -> 24 -> 14 -> 9 by adding real tests rather than allowlist entries; these nine are the residue where a test would assert the exit code and nothing else. 2026-08-24.",
  "tools/filler_baseline.py::main::RETURN_NONE::c4777b5d": "filler_baseline.py CLI plumbing: process exit codes, the sys.path bootstrap, and the `return 1` / `return 0` arms of already-tested error paths. The BEHAVIOUR these sit on is pinned elsewhere -- collect() has zero survivors, and the printed table, --json, --rank, --top, the ambiguous-match refusal and the not-found refusal all have tests asserting their content. What survives here is whether the process exits 0 or 1 after printing the right thing, which no consumer of this tool branches on: it is read by a human and by /debrief, both of which consume stdout. Mutants were driven 32 -> 24 -> 14 -> 9 by adding real tests rather than allowlist entries; these nine are the residue where a test would assert the exit code and nothing else. 2026-08-24.",
  "tools/granola_save.py::attribution_warning::IF_FALSE::bfd8fca2": "`if not total: return None` in attribution_warning is UNREACHABLE, established by trying to kill it rather than by inspection. To reach it, split_transcript_turns() must return a non-empty turn list whose turns contain zero words -- impossible, because that function skips empty bodies (`if not body: continue`), so every turn it returns has at least one word. A transcript of bare labels (`Me:  Them:`) returns ([], []) and exits at the earlier no-labels branch, never reaching this line; test_transcript_with_labels_but_no_words_does_not_crash asserts exactly that path and is kept for the no-crash guarantee it pins. The guard stays because the helper is module-level and a future caller may not share that precondition. Same shape and same reasoning as the twin entry in filler_baseline.py::_collapsed_channel_reason. 2026-08-25.",
  "tools/handoff_state.py::<module>::DROP_CALL::0d29993b": "Drops `sys.exit(main(...))`, the CLI entrypoint, for the same reason as the IF_FALSE entry above. 2026-08-25.",
  "tools/handoff_state.py::<module>::IF_FALSE::dd9abcf0": "IF_FALSE on `if __name__ == \"__main__\":` suppresses the CLI entrypoint. Every test imports handoff_state as a module and calls scan/state_block/write directly, so this branch is not on any tested path. Equivalent mutant for the properties under test. 2026-08-25.",
  "tools/handoff_state.py::main::DROP_CALL::2e6ce903": "Drops the --write argparse registration. The behaviour that matters (replace / insert / refuse-without-anchor, and that the returned block is the one written to disk) is covered directly against write() in test_handoff_state_block.py, which does not go through argparse. A missing flag is a usage error surfaced instantly on the next run, never a silent wrong number in the handoff, which is the one failure this tool exists to prevent. 2026-08-25.",
  "tools/handoff_state.py::main::DROP_CALL::6d9b8d30": "Drops the --write argparse registration. The behaviour that matters (replace / insert / refuse-without-anchor, and that the returned block is the one written to disk) is covered directly against write() in test_handoff_state_block.py, which does not go through argparse. A missing flag is a usage error surfaced instantly on the next run, never a silent wrong number in the handoff, which is the one failure this tool exists to prevent. 2026-08-25.",
  "tools/handoff_state.py::main::RETURN_NONE::06d3d9b2": "Changes main()'s exit code from 0 to None. This module is invoked by hand and by the test suite via its functions, never by a hook or launchd job that branches on exit status, so no consumer reads it. If one is ever added, this entry should be removed and the mutant killed. 2026-08-25.",
  "tools/learning_ledger.py::<module>::DROP_CALL::0d29993b": "Drops `sys.exit(main(sys.argv[1:]))`, the CLI entrypoint, for the same reason as the IF_FALSE entry above. main()'s return value is asserted directly in test_main_returns_0_on_a_successful_read and test_main_rejects_a_missing_memory_dir. 2026-08-25.",
  "tools/learning_ledger.py::<module>::IF_FALSE::dd9abcf0": "IF_FALSE on `if __name__ == \"__main__\":` suppresses the CLI entrypoint; every test imports learning_ledger and calls collect/read_*/main directly. Same shape as the accepted promotion_schema.py, check_hook_warn_tier.py, detector_probe.py and detector_run.py entries. 2026-08-25.",
  "tools/learning_ledger.py::read_antipatterns::DROP_CONTINUE::eee0bbba": "Structurally equivalent `continue` statements in learning_ledger's line loops (read_lessons, read_antipatterns) and the two guards they sit behind. Each `continue` is the last statement of its branch, and every branch it could fall through into is itself a skip: a line before any section heading has no cells to parse, a separator row returns [] from _row_cells, and a row with too few cells fails its length test. Dropping the continue therefore reaches the same no-record outcome by a longer path, which no caller can observe. Equivalent by argument. The branches that were NOT equivalent were killed with real tests rather than allowlisted: the section-1/section-2 boundary, the header rows, the too-few-cells rows, the stray status line, and the due-count boundary at exactly 2. Survivors went 18 -> 13 -> these. 2026-08-25.",
  "tools/learning_ledger.py::read_lessons::DROP_CONTINUE::eee0bbba": "Structurally equivalent `continue` statements in learning_ledger's line loops (read_lessons, read_antipatterns) and the two guards they sit behind. Each `continue` is the last statement of its branch, and every branch it could fall through into is itself a skip: a line before any section heading has no cells to parse, a separator row returns [] from _row_cells, and a row with too few cells fails its length test. Dropping the continue therefore reaches the same no-record outcome by a longer path, which no caller can observe. Equivalent by argument. The branches that were NOT equivalent were killed with real tests rather than allowlisted: the section-1/section-2 boundary, the header rows, the too-few-cells rows, the stray status line, and the due-count boundary at exactly 2. Survivors went 18 -> 13 -> these. 2026-08-25.",
  "tools/learning_ledger.py::read_lessons::IF_FALSE::27ea3785": "Structurally equivalent `continue` statements in learning_ledger's line loops (read_lessons, read_antipatterns) and the two guards they sit behind. Each `continue` is the last statement of its branch, and every branch it could fall through into is itself a skip: a line before any section heading has no cells to parse, a separator row returns [] from _row_cells, and a row with too few cells fails its length test. Dropping the continue therefore reaches the same no-record outcome by a longer path, which no caller can observe. Equivalent by argument. The branches that were NOT equivalent were killed with real tests rather than allowlisted: the section-1/section-2 boundary, the header rows, the too-few-cells rows, the stray status line, and the due-count boundary at exactly 2. Survivors went 18 -> 13 -> these. 2026-08-25.",
  "tools/learning_ledger.py::read_lessons::IF_FALSE::e4cb9dd8": "Structurally equivalent `continue` statements in learning_ledger's line loops (read_lessons, read_antipatterns) and the two guards they sit behind. Each `continue` is the last statement of its branch, and every branch it could fall through into is itself a skip: a line before any section heading has no cells to parse, a separator row returns [] from _row_cells, and a row with too few cells fails its length test. Dropping the continue therefore reaches the same no-record outcome by a longer path, which no caller can observe. Equivalent by argument. The branches that were NOT equivalent were killed with real tests rather than allowlisted: the section-1/section-2 boundary, the header rows, the too-few-cells rows, the stray status line, and the due-count boundary at exactly 2. Survivors went 18 -> 13 -> these. 2026-08-25.",
  "tools/meeting_vocab.py::_named_speaker_turns::IF_FALSE::61203292": "`if not owner and not other: return None` after the split loop. Reaching it requires every body behind a >=3-occurrence label to be empty, which no real transcript produces and which the earlier speaker-frequency floor makes vanishingly unlikely. Returning None rather than ([], []) here would be indistinguishable to the caller anyway -- split_transcript_turns converts a None to ([], []) on the next line -- so the mutant is also semantically equivalent from every caller's view. 2026-08-25.",
  "tools/meeting_vocab.py::_named_speaker_turns::IF_FALSE::625d9f7f": "`if len(parts) < 3: return None` after the speaker set is established. Unreachable by construction: the function has already required two distinct labels each matching _NAMED_TURN_RE at least three times, so the same regex splitting the same text cannot yield fewer than three parts. Kept because the two operations are textually far apart and a future edit to either could break the invariant silently. 2026-08-25.",
  "tools/meeting_vocab.py::split_transcript_turns::IF_FALSE::625d9f7f": "`if len(parts) < 3: return [], []` is an early-exit guard for text with no recognised speaker label. Forcing it false does not change the result: _TURN_RE.split() on unlabelled text returns a 1-element list, so the loop below runs `range(1, 0, 2)`, which is empty, and the function returns the same ([], []) by falling through. Proven equivalent rather than assumed: test_unlabelled_text_returns_two_empty_lists_not_a_crash asserts that exact return and passes with the mutant applied, which is what identifies it as equivalent instead of uncovered. The guard is kept for readability, and because it makes the parse-failure contract explicit at the top of the function where a reader looks for it. The OTHER survivor in this function (`if not body:`) was NOT allowlisted - it was killed by test_empty_turn_bodies_are_skipped_not_counted_as_turns. 2026-08-24.",
  "tools/promotion_schema.py::<module>::DROP_CALL::0d29993b": "Drops `sys.exit(main(sys.argv[1:]))`, the CLI entrypoint, for the same reason as the IF_FALSE entry above. The exit code that matters (2 blocks, 0 passes, 1 on usage error) is asserted directly against main() in test_main_blocks_with_exit_2_on_a_violation and its siblings, which do not go through the module guard. 2026-08-25.",
  "tools/promotion_schema.py::<module>::IF_FALSE::dd9abcf0": "IF_FALSE on `if __name__ == \"__main__\":` suppresses the CLI entrypoint. Every test imports promotion_schema as a module and calls scan/check_file/main directly, so this branch is on no tested path. Same shape and same reasoning as the accepted handoff_state.py entry. 2026-08-25.",
  "tools/promotion_schema.py::parse_frontmatter::DROP_CONTINUE::eee0bbba": "Drops the `continue` in that same blank/comment branch, which is the other half of the mutant above and equivalent for the same reason: control falls through to a regex that provably cannot match a blank or comment line, then hits the `if not m: continue` on the following branch and skips the line anyway. Note the sibling DROP_CONTINUE, after `if not m`, was NOT allowlisted: dropping that one dereferences None, and it is killed by test_a_non_key_value_line_is_skipped_without_crashing. 2026-08-25.",
  "tools/promotion_schema.py::parse_frontmatter::IF_FALSE::11b43dfa": "The blank/comment skip in parse_frontmatter is a pure short-circuit, and the mutant is equivalent by argument rather than by assumption. Forcing the condition False sends blank and comment lines on to the key regex, ^\\s*([A-Za-z_][A-Za-z0-9_]*)\\s*:, which requires a letter or underscore as the first non-space character. A blank line offers none, and a comment line offers '#', so neither can match and both fall to the same `continue` on the next branch. There is no input for which skipping early and matching-then-failing produce different dicts. The guard is kept because it states the intent (comments are not keys) at the site a future edit to the regex would otherwise silently break. 2026-08-25.",
  "tools/scan_promotion_candidates.py::oversized_context_files::DROP_CONTINUE::eee0bbba": "The is_file() guards and the OSError/continue arm in oversized_context_files together handle one condition: a target that vanishes between the is_file() check and the stat() call. Forcing either IF_TRUE registers an absent path, whose stat() then raises OSError and is swallowed by that same continue, so the returned list is identical -- equivalent by argument. The DROP_CONTINUE is reachable only from that same race. Killing these needs a mocked filesystem race, which would pin an internal call sequence rather than any behaviour a caller can observe. The branches this session actually added (is_frozen_shard, the frozen filter, the CLAUDE.md target, and the size > limit comparison) were all killed with real tests rather than allowlisted: survivors in this function went 6 -> 3. 2026-08-25.",
  "tools/scan_promotion_candidates.py::oversized_context_files::IF_TRUE::9e4ae0c4": "The is_file() guards and the OSError/continue arm in oversized_context_files together handle one condition: a target that vanishes between the is_file() check and the stat() call. Forcing either IF_TRUE registers an absent path, whose stat() then raises OSError and is swallowed by that same continue, so the returned list is identical -- equivalent by argument. The DROP_CONTINUE is reachable only from that same race. Killing these needs a mocked filesystem race, which would pin an internal call sequence rather than any behaviour a caller can observe. The branches this session actually added (is_frozen_shard, the frozen filter, the CLAUDE.md target, and the size > limit comparison) were all killed with real tests rather than allowlisted: survivors in this function went 6 -> 3. 2026-08-25.",
  "tools/scan_promotion_candidates.py::oversized_context_files::IF_TRUE::b9b9f997": "The is_file() guards and the OSError/continue arm in oversized_context_files together handle one condition: a target that vanishes between the is_file() check and the stat() call. Forcing either IF_TRUE registers an absent path, whose stat() then raises OSError and is swallowed by that same continue, so the returned list is identical -- equivalent by argument. The DROP_CONTINUE is reachable only from that same race. Killing these needs a mocked filesystem race, which would pin an internal call sequence rather than any behaviour a caller can observe. The branches this session actually added (is_frozen_shard, the frozen filter, the CLAUDE.md target, and the size > limit comparison) were all killed with real tests rather than allowlisted: survivors in this function went 6 -> 3. 2026-08-25.",
  "tools/prepush_pii_guard.py::<module>::DROP_CALL::16811be8": "Drops `sys.path.insert(0, dirname(__file__))`. EQUIVALENT: the tool is only ever executed as a script (pre-push hook, or by hand), and Python already puts the script's own directory at sys.path[0], so `import check_public_pii` resolves identically with the line gone. Verified 2026-08-26.",
  "tools/prepush_pii_guard.py::tracked_public_files::IF_FALSE::719d7377": "Forces the is_gitignored() skip off. DEAD BRANCH, not a coverage gap: the source is `git ls-files`, which lists TRACKED paths only, and `git check-ignore` reports a tracked path as NOT ignored even when it matches .gitignore (verified empirically 2026-08-26). The condition can therefore never be True here. What actually keeps data/ and output/ out of the scan is that they are untracked and so never listed. Pinned by test_an_ignored_untracked_file_is_never_in_the_pushed_tree.",
  "tools/prepush_pii_guard.py::tracked_public_files::DROP_CONTINUE::eee0bbba": "Drops the `continue` inside the same dead is_gitignored branch. Unreachable for the reason above, so no input can distinguish it.",
  "tools/prepush_pii_guard.py::tracked_public_files::IF_TRUE::f79f8a06": "Forces `(root / rel).is_file()` true, appending tracked-but-absent paths. EQUIVALENT: the caller reads each file inside `try: ... except Exception: continue`, so a missing path is skipped one step later with the same outcome. Covered from the outside by test_a_tracked_file_missing_from_disk_does_not_crash_the_fallback.",
  "tools/prepush_pii_guard.py::public_blobs_at::IF_FALSE::719d7377": "Same dead is_gitignored branch on the sha path. The source is `git ls-tree`, which yields only paths present in the commit -- tracked by construction -- so check-ignore never reports them ignored. See the tracked_public_files entry.",
  "tools/prepush_pii_guard.py::public_blobs_at::DROP_CONTINUE::eee0bbba": "Drops the `continue` inside that same unreachable branch.",
  "tools/prepush_pii_guard.py::main::RETURN_NONE::06d3d9b2": "Turns `return 0` into `return None` on the three ALLOW paths (no denylist, scan clean, override). EQUIVALENT: main()'s value goes to sys.exit(), and sys.exit(None) exits 0 -- identical to sys.exit(0). NOTE this key does NOT cover the block path: `return 1` -> None WOULD flip a block into an allow, and that mutant is killed by test_a_hit_blocks_with_a_nonzero_exit.",
  "tools/prepush_pii_guard.py::main::DROP_CONTINUE::eee0bbba": "Drops the `continue` in the working-tree fallback's `except Exception:` handler. Effectively unreachable: read_text uses errors='replace', so decoding cannot raise, and non-files were already filtered by the is_file() check above.",
  "tools/prepush_pii_guard.py::<module>::IF_TRUE::dd9abcf0": "Forces the `if __name__ == \"__main__\":` guard true. EQUIVALENT under every path that executes this file: it is run as a script, where the guard is already true. It would only differ on import, and nothing imports this module.",
  "tools/prepush_pii_guard.py::<module>::DROP_CALL::a5b711fb": "Drops `sys.exit(0)` in the fail-open exception handler. EQUIVALENT: it is the last statement, so control falls off the end of the module and the interpreter exits 0 anyway. The visible half of that handler -- printing WHY the push was allowed -- is a separate mutant and IS killed, by test_an_infrastructure_failure_allows_the_push_and_says_why."
}
