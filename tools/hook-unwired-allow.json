{
  "check_plan_partner_critique.py": "CLI-only, declared 2026-09-06 (Nick's call). Two reasons, the second one load-bearing. (1) It writes one line to stderr and has no sys.exit(2) or return 2 anywhere, so wiring it would put a gate on a channel Claude Code does not surface -- the exact shape feedback_warn_vs_block_hook_design (occurrences 5) exists to stop, and check_hook_warn_tier would flag it as a warn-tier violation the moment it was wired. (2) It is REDUNDANT with an enforced gate that already covers the same artifact class: cross_model_gate.py:60-62 matches output/analysis/.*(handoff|build-log|plan|PLAN) and requires a cross-model verification before push, failing closed with a ledger row. check_plan_partner_critique watches an overlapping set (output/**/*-plan*.md) and produces no record at all. Adding an inert second mechanism over an artifact class that already has an enforced one makes the enforced one look optional. OPEN QUESTION Nick raised 2026-09-06 and has not settled: whether the mckinsey-critical-advisor partner lens named by feedback_partner_pressuretest_above_10hr is a DISTINCT requirement from a Codex cross-model verification, or satisfied by it. /critique-plan runs six lenses (completeness, risk, codebase alignment, simplicity, sequencing, independent planner) and carries no partner lens. If the answer is distinct, the surface is a /critique-plan Agent 7 plus a cross_model_gate domain, not this hook -- and this entry should be removed rather than edited.",
  "check_status_query_verification.py": "BUILT AND BOUNDED, DELIBERATELY NOT WIRED, 2026-09-07. Nick's call after a second adversarial review. The RUNTIME contract is sound and tested: bounded to 2 seconds and 1 MiB with the cap enforced at the read request, no unbounded fallback on any error path, exit 0 and silent stderr on a closed consumer, 93 tests, mutation 36 killed / 0 survived. What is NOT established is TRIGGER PRECISION, and that is the whole point of the hook. Two rounds of external review found it failing in BOTH directions at P0: it stays quiet on real status questions ('Where are we going with the migration?', 'Where are we standing on the audit?') and fires on ordinary work ('Is there anything we should rename?', 'Before I start implementing, add a test.', 'Before we leave for lunch, save the file.'). A hook that misses what it exists for AND nags about everything else is worse than none, because it trains the reader to ignore it. Both times the precision claim rested on hand-picked counterexamples chosen by the author, which the reviewer named directly: the tests 'prove that one rescued phrase, not the claimed distinction'. WHAT WOULD CHANGE THIS: not a third regex pass. Precision has to be measured against a real corpus of this user's own prompts (the transcripts under ~/.claude/projects/) with a labelled sample, the same way restraint was measured for the two gates wired in 6bde792 -- or the regex approach abandoned for one that can be evaluated. Tracked in data/job-todos.md. Remove this entry when precision is measured, not when the patterns merely look better."
}
