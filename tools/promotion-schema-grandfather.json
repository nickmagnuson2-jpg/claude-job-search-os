{
  "feedback_a_declarative_field_with_no_reader_is_documentation.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25). A date IS recoverable from the promoted: string (2026-08-21) and should be backfilled into promoted_date when this file is next touched; the exit path is not recorded anywhere and must be re-derived from the named artifact. Legacy debt, counted rather than hidden.",
  "feedback_a_new_toplevel_dir_inherits_no_protections.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25). A date IS recoverable from the promoted: string (2026-08-17) and should be backfilled into promoted_date when this file is next touched; the exit path is not recorded anywhere and must be re-derived from the named artifact. Legacy debt, counted rather than hidden.",
  "feedback_allowlist_scope_undercovers_by_construction.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25). A date IS recoverable from the promoted: string (2026-08-18) and should be backfilled into promoted_date when this file is next touched; the exit path is not recorded anywhere and must be re-derived from the named artifact. Legacy debt, counted rather than hidden.",
  "feedback_audit_frame_structure_not_only_claim_truth.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_blind_sighted_pair_for_self_audit.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_calibrate_drills_to_candidate_baseline.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_capture_as_reflection_means_verbatim_by_default.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_carry_the_spec_into_every_review_round.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_casual_register_proper_capitalization.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_check_interviewer_excluded_the_proof_domain.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_checkout_accomplishments_reads_reflections.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_claimed_fix_is_not_a_closed_defect.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25). A date IS recoverable from the promoted: string (2026-08-21) and should be backfilled into promoted_date when this file is next touched; the exit path is not recorded anywhere and must be re-derived from the named artifact. Legacy debt, counted rather than hidden.",
  "feedback_classifier_format_contract_brittle.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_cold_outreach_flow_gaps.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25). A date IS recoverable from the promoted: string (2026-08-25) and should be backfilled into promoted_date when this file is next touched; the exit path is not recorded anywhere and must be re-derived from the named artifact. Legacy debt, counted rather than hidden.",
  "feedback_consolidate_duplicated_domain_logic_and_verify_on_real_data.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_content_over_voice_bifurcate_corpus.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_count_failure_family_n_for_promotion.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_credit_only_real_interactions_in_outreach.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_cv_em_dash_and_source_verb_regression.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_cv_font_off_check_line_spacing_first.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_cv_one_page_default_no_skills_fluff.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_cv_summary_must_be_standable.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_debrief_add_signal_analysis_step.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_debrief_networking_call_mode.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_defer_todos_as_reopen_gates.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_denylist_needs_slug_forms_not_just_names.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_detect_data_defects_at_capture_time.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25). A date IS recoverable from the promoted: string (2026-08-25) and should be backfilled into promoted_date when this file is next touched; the exit path is not recorded anywhere and must be re-derived from the named artifact. Legacy debt, counted rather than hidden.",
  "feedback_dev_jargon_to_ceo_context.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_diagnose_unexpected_state_before_closing.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_dictation_first_for_self_positioning_emails.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_doc_tracked_vs_tool_tracked_state.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_documenting_a_leak_can_recommit_it.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25). A date IS recoverable from the promoted: string (2026-08-19) and should be backfilled into promoted_date when this file is next touched; the exit path is not recorded anywhere and must be re-derived from the named artifact. Legacy debt, counted rather than hidden.",
  "feedback_dont_add_mandatory_ceremony_unasked.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_dont_archive_hybrid_before_kernel_promoted.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_dont_infer_nick_role_titles_from_context.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_draft_archive_is_not_proof_of_send.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_email_paste_workflow.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_encode_uncertainty_in_the_artifact_form.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_entity_identifier_drift_breaks_reconciliation.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_fetch_and_diff_before_push_to_diverged_remote.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_forward_beat_belongs_to_the_process_owner.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25). A date IS recoverable from the promoted: string (2026-08-24) and should be backfilled into promoted_date when this file is next touched; the exit path is not recorded anywhere and must be re-derived from the named artifact. Legacy debt, counted rather than hidden.",
  "feedback_frame_nick_builds_as_building_not_side_project.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_friction_log_hook_blind_spots.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_generalize_examples_in_public_artifacts.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_give_nick_beats_not_a_polished_script.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_glob_resolve_screenshot_path_never_hand_type.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_graceful_acknowledgment_of_soft_signals.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_granola_autotitle_unreliable_for_classification.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_grep_is_ripgrep_gitignore_blindspot.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_ground_email_draft_in_verbatim_source_not_paraphrase.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25). A date IS recoverable from the promoted: string (2026-08-13) and should be backfilled into promoted_date when this file is next touched; the exit path is not recorded anywhere and must be re-derived from the named artifact. Legacy debt, counted rather than hidden.",
  "feedback_ground_pressure_test_in_decision_panel_not_generic_partner.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_guard_must_hard_abort_on_empty_input.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25). A date IS recoverable from the promoted: string (2026-08-14) and should be backfilled into promoted_date when this file is next touched; the exit path is not recorded anywhere and must be re-derived from the named artifact. Legacy debt, counted rather than hidden.",
  "feedback_guard_tests_must_cover_shipped_surface.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25). A date IS recoverable from the promoted: string (2026-08-19) and should be backfilled into promoted_date when this file is next touched; the exit path is not recorded anywhere and must be re-derived from the named artifact. Legacy debt, counted rather than hidden.",
  "feedback_guard_the_fact_not_the_process.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25). A date IS recoverable from the promoted: string (2026-08-13) and should be backfilled into promoted_date when this file is next touched; the exit path is not recorded anywhere and must be re-derived from the named artifact. Legacy debt, counted rather than hidden.",
  "feedback_hook_fp_surface_depends_on_hook_type.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25). A date IS recoverable from the promoted: string (2026-08-13) and should be backfilled into promoted_date when this file is next touched; the exit path is not recorded anywhere and must be re-derived from the named artifact. Legacy debt, counted rather than hidden.",
  "feedback_hook_self_trigger_in_documentation.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_index_decay_needs_maintenance_trigger.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_infrastructure_during_search.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25). A date IS recoverable from the promoted: string (2026-06-12) and should be backfilled into promoted_date when this file is next touched; the exit path is not recorded anywhere and must be re-derived from the named artifact. Legacy debt, counted rather than hidden.",
  "feedback_interview_opener_components_not_finished_line.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_invite_feedback_when_sharing_own_work.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_judgment_as_wedge.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_ledger_dodge_closers_in_header.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_light_availability_close_over_scheduling_offer.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_llm_self_policing_fails.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_llm_verification_system.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25). A date IS recoverable from the promoted: string (2026-08-13) and should be backfilled into promoted_date when this file is next touched; the exit path is not recorded anywhere and must be re-derived from the named artifact. Legacy debt, counted rather than hidden.",
  "feedback_manual_first_informs_system.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_marketplace_recruiter_fit_profile_pattern.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_measure_before_restructuring_a_thesis.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25). A date IS recoverable from the promoted: string (2026-08-25) and should be backfilled into promoted_date when this file is next touched; the exit path is not recorded anywhere and must be re-derived from the named artifact. Legacy debt, counted rather than hidden.",
  "feedback_minimize_polish_on_voice_pure_dictation.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_model_concept_before_numbers.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_morning_starter_doc_pattern.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_multipass_independent_review.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_name_a_rule_by_its_subject_not_its_internal_id.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25). A date IS recoverable from the promoted: string (2026-08-16) and should be backfilled into promoted_date when this file is next touched; the exit path is not recorded anywhere and must be re-derived from the named artifact. Legacy debt, counted rather than hidden.",
  "feedback_name_the_scope_before_stating_the_conclusion.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25). A date IS recoverable from the promoted: string (2026-08-13) and should be backfilled into promoted_date when this file is next touched; the exit path is not recorded anywhere and must be re-derived from the named artifact. Legacy debt, counted rather than hidden.",
  "feedback_never_claim_a_write_without_verifying_it.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_never_forbid_the_verifying_question.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25). A date IS recoverable from the promoted: string (2026-08-13) and should be backfilled into promoted_date when this file is next touched; the exit path is not recorded anywhere and must be re-derived from the named artifact. Legacy debt, counted rather than hidden.",
  "feedback_never_modify_guard_hook_to_unblock_self.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25). A date IS recoverable from the promoted: string (2026-08-14) and should be backfilled into promoted_date when this file is next touched; the exit path is not recorded anywhere and must be re-derived from the named artifact. Legacy debt, counted rather than hidden.",
  "feedback_never_self_mutate_a_source_rewriting_tool.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25). A date IS recoverable from the promoted: string (2026-08-19) and should be backfilled into promoted_date when this file is next touched; the exit path is not recorded anywhere and must be re-derived from the named artifact. Legacy debt, counted rather than hidden.",
  "feedback_no_batch_write_then_consume.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_no_confabulation_in_agent_prompts.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_no_confabulation_in_corpus_synthesis.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_no_if_you_have_a_sec_scaffold.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_no_load_bearing_vocabulary.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25). A date IS recoverable from the promoted: string (2026-08-20) and should be backfilled into promoted_date when this file is next touched; the exit path is not recorded anywhere and must be re-derived from the named artifact. Legacy debt, counted rather than hidden.",
  "feedback_no_product_docs_to_employees.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25). A date IS recoverable from the promoted: string (2026-08-13) and should be backfilled into promoted_date when this file is next touched; the exit path is not recorded anywhere and must be re-derived from the named artifact. Legacy debt, counted rather than hidden.",
  "feedback_no_shape_of_role_phrasing.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_opener_states_a_fact_not_a_verdict.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25). A date IS recoverable from the promoted: string (2026-08-24) and should be backfilled into promoted_date when this file is next touched; the exit path is not recorded anywhere and must be re-derived from the named artifact. Legacy debt, counted rather than hidden.",
  "feedback_orchestrator_cannot_close_its_own_findings.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_output_over_engine_test.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_outreach_value_prop_grounded_not_generic.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_partner_mode_silent_mode_for_voice_work.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_partner_pressuretest_above_10hr.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_passing_test_can_encode_wrong_oracle.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_persona_sim_prompts_need_structural_role_binding.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25). A date IS recoverable from the promoted: string (2026-08-20) and should be backfilled into promoted_date when this file is next touched; the exit path is not recorded anywhere and must be re-derived from the named artifact. Legacy debt, counted rather than hidden.",
  "feedback_pii_denylist_only_sees_its_source_files.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_pii_subagents_rationalize_examples.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_pii_sweep_must_cover_siblings_and_history.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_pre_name_self_doubt_firings_in_prep.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_precheck_plans_against_memory_rules.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_prefer_extending_existing_infra_over_new_build.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_prefer_gmail_fetch_script_over_mcp.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_prep_artifact_not_consumed.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_prep_interview_must_advance_pipeline_on_scheduled_round.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_print_prep_pdfs.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_productize_recurring_adhoc_patterns.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_project_files_overclaim_vector.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_promotion_must_sweep_enforcement_tier.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_pulled_corpus_must_match_claimed_topic.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_push_back_on_grey_areas_before_starting.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_qualitative_vs_binary_verification.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_reconcile_stale_gate_against_specific_intel.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_reflection_event_date_not_capture_date.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_reflection_wiki_links.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_reglob_newest_races_against_pinned_target.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_remember_deferred_goes_to_inbox.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25). A date IS recoverable from the promoted: string (2026-08-18) and should be backfilled into promoted_date when this file is next touched; the exit path is not recorded anywhere and must be re-derived from the named artifact. Legacy debt, counted rather than hidden.",
  "feedback_repairs_are_local_defects_are_distributed.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_replace_all_substring_check.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_route_research_subagents_through_exa.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_sampled_check_needs_a_conservation_anchor.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_save_interesting_routes_to_source_articles.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_self_graded_gates_answer_from_context_not_artifact.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25). A date IS recoverable from the promoted: string (2026-08-25) and should be backfilled into promoted_date when this file is next touched; the exit path is not recorded anywhere and must be re-derived from the named artifact. Legacy debt, counted rather than hidden.",
  "feedback_show_concrete_over_told_positioning_in_outreach.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_silent_gap_filling_on_screenshot_parsing.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_similarity_is_not_equivalence_in_bridge_claims.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_smoke_test_catches_unit_test_blind_spots.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_stale_todo_and_networking_closure_gaps.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_surface_scheduled_events_in_ranking.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25). A date IS recoverable from the promoted: string (2026-08-23) and should be backfilled into promoted_date when this file is next touched; the exit path is not recorded anywhere and must be re-derived from the named artifact. Legacy debt, counted rather than hidden.",
  "feedback_synthesis_prompts_mandate_pushback_and_dontbuild.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_therapy_adjacent_routing.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25). A date IS recoverable from the promoted: string (2026-08-13) and should be backfilled into promoted_date when this file is next touched; the exit path is not recorded anywhere and must be re-derived from the named artifact. Legacy debt, counted rather than hidden.",
  "feedback_todo_write_done_misses_misfiled_rows.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25). A date IS recoverable from the promoted: string (2026-08-17) and should be backfilled into promoted_date when this file is next touched; the exit path is not recorded anywhere and must be re-derived from the named artifact. Legacy debt, counted rather than hidden.",
  "feedback_todos_full_file_read_required.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_two_tier_capture.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_validate_on_the_contested_subset.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25). A date IS recoverable from the promoted: string (2026-08-13) and should be backfilled into promoted_date when this file is next touched; the exit path is not recorded anywhere and must be re-derived from the named artifact. Legacy debt, counted rather than hidden.",
  "feedback_verify_agent_infrastructure_claims_before_relaying.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25). A date IS recoverable from the promoted: string (2026-08-13) and should be backfilled into promoted_date when this file is next touched; the exit path is not recorded anywhere and must be re-derived from the named artifact. Legacy debt, counted rather than hidden.",
  "feedback_verify_agent_research_before_propagation.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_verify_and_present_review_findings.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25). A date IS recoverable from the promoted: string (2026-08-14) and should be backfilled into promoted_date when this file is next touched; the exit path is not recorded anywhere and must be re-derived from the named artifact. Legacy debt, counted rather than hidden.",
  "feedback_verify_auto_capture_actually_fires.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_verify_mutation_via_raw_file_not_tool_subcommand.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_verify_the_surface_fires_before_anchoring_to_it.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25). A date IS recoverable from the promoted: string (2026-08-14) and should be backfilled into promoted_date when this file is next touched; the exit path is not recorded anywhere and must be re-derived from the named artifact. Legacy debt, counted rather than hidden.",
  "feedback_voice_anchor_pass_at_iteration_3.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_voice_corpus_register_transfer_and_contamination.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_voice_export_adversarial_and_persona_variants.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_voice_pure_blockquote_format.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_voice_pure_diff_minimal.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25). A date IS recoverable from the promoted: string (2026-08-13) and should be backfilled into promoted_date when this file is next touched; the exit path is not recorded anywhere and must be re-derived from the named artifact. Legacy debt, counted rather than hidden.",
  "feedback_voice_pure_email_questions_learn_from_iterations.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_volume_as_cope_yellow_flag.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_warn_vs_block_hook_design.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_watchdog_must_live_outside_what_it_watches.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25), and the promoted: string carries NO recoverable date, so this promotion left no observable record at all. Both fields must be re-derived from the named artifact when this file is next touched. Legacy debt, counted rather than hidden.",
  "feedback_workflow_args_must_be_object_not_prose.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25). A date IS recoverable from the promoted: string (2026-08-19) and should be backfilled into promoted_date when this file is next touched; the exit path is not recorded anywhere and must be re-derived from the named artifact. Legacy debt, counted rather than hidden.",
  "feedback_zuora_principal_title_is_cpto.md": "Promoted before the promoted_date/exit_path fields existed (added 2026-08-25). A date IS recoverable from the promoted: string (2026-08-24) and should be backfilled into promoted_date when this file is next touched; the exit path is not recorded anywhere and must be re-derived from the named artifact. Legacy debt, counted rather than hidden."
}