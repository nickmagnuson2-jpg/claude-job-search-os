---
phase: 01-granola-mcp-integration-call-analysis-coaching-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/debrief/SKILL.md
autonomous: false
requirements: [R1.1, R1.2, R1.3, R1.4, R1.5, R1.6, R1.7, R1.8]
user_setup:
  - service: granola-mcp
    why: "OAuth browser authentication required for Granola MCP"
    env_vars: []
    dashboard_config:
      - task: "Complete browser OAuth when prompted by claude mcp add"
        location: "Browser popup during MCP configuration"
  - service: granola-plan
    why: "Transcript access requires paid Granola plan (Business or Enterprise)"
    env_vars: []
    dashboard_config:
      - task: "Verify Granola subscription tier supports transcript access"
        location: "Granola app settings or granola.ai account page"

must_haves:
  truths:
    - "User can invoke /debrief without pasting a transcript and get a Granola meeting picker"
    - "Picker displays meeting title, date, and duration for recent meetings"
    - "After selecting a meeting, the full debrief analysis runs on the Granola transcript"
    - "Filler words (really, kind of, definitely, to be honest with you, absolutely, pretty) are counted per-call"
    - "Anti-pattern tracker is updated with per-call data after each debrief"
    - "Company notes file is appended with structured call intel after debrief"
    - "Existing paste-transcript flow still works unchanged"
  artifacts:
    - path: ".claude/skills/debrief/SKILL.md"
      provides: "Extended debrief skill with Granola MCP fetch, company-notes append, enhanced filler tracking"
      contains: "list_meetings"
  key_links:
    - from: ".claude/skills/debrief/SKILL.md"
      to: "Granola MCP server"
      via: "list_meetings and get_meeting_transcript tool calls"
      pattern: "list_meetings|get_meeting_transcript"
    - from: ".claude/skills/debrief/SKILL.md"
      to: "data/company-notes/"
      via: "Write tool appending call intel"
      pattern: "data/company-notes"
---

<objective>
Configure the Granola MCP server in this project, discover its API surface, and extend the existing `/debrief` skill to fetch transcripts from Granola as an alternative to pasting. Add company-notes auto-append and enhanced filler word tracking to the debrief flow.

Purpose: Every interview call recorded by Granola becomes instantly debriefable without copy-paste. Call intel flows into company notes and anti-pattern tracking automatically.
Output: Updated SKILL.md with Granola integration, working MCP connection.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-granola-mcp-integration-call-analysis-coaching-loop/01-CONTEXT.md
@.planning/phases/01-granola-mcp-integration-call-analysis-coaching-loop/01-RESEARCH.md

@.claude/skills/debrief/SKILL.md
@coaching/anti-pattern-tracker.md
@framework/templates/recruiter-session.md

<interfaces>
<!-- Granola MCP tools (from research) -->
- list_meetings: Returns meeting metadata (title, date, duration, ID)
- get_meeting_transcript: Returns transcript segments with {speaker: {source: "speaker"|"microphone"}, text: "..."}
- get_meetings: Batch retrieval by document IDs
- query_granola_meetings: Natural language search with citations
- list_meeting_folders: Enumerate folders (paid only)

<!-- Transcript segment format -->
{"transcript": [{"speaker": {"source": "speaker"}, "text": "..."}, {"speaker": {"source": "microphone"}, "text": "..."}]}

<!-- Speaker source mapping -->
- source: "microphone" = the user (Nick)
- source: "speaker" = the other participant (interviewer/recruiter)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Configure Granola MCP server and discover API surface</name>
  <files>MCP configuration (via claude mcp add command)</files>
  <action>
User must configure Granola MCP server and complete browser OAuth. This cannot be automated because MCP OAuth requires interactive browser authentication.

1. Run: `claude mcp add granola --transport http https://mcp.granola.ai/mcp`
2. Complete the browser OAuth flow when prompted
3. Verify the MCP is configured by checking that Granola tools appear in the Claude Code session
4. Test by calling `list_meetings` to confirm meetings are returned with title, date, and duration fields
5. Test by calling `get_meeting_transcript` on one meeting to confirm transcript segments are returned with speaker.source and text fields
6. If `get_meeting_transcript` returns empty or errors, verify your Granola plan supports transcript access (Business or Enterprise tier required)
  </action>
  <verify>
    <automated>claude mcp list 2>/dev/null | grep -ci granola || echo "0"</automated>
  </verify>
  <done>Granola MCP is configured, list_meetings returns meetings with title/date/duration fields, get_meeting_transcript returns transcript segments with speaker.source and text fields.</done>
  <what-built>Granola MCP must be added to the project so the debrief skill can call Granola tools. This requires browser-based OAuth that only the user can complete.</what-built>
  <how-to-verify>
    1. Run: `claude mcp add granola --transport http https://mcp.granola.ai/mcp`
    2. Complete the browser OAuth flow when prompted
    3. Verify the MCP is configured by checking that Granola tools appear in the Claude Code session
    4. Test by calling `list_meetings` to confirm meetings are returned with title, date, and duration fields
    5. Test by calling `get_meeting_transcript` on one meeting to confirm transcript segments are returned with speaker.source and text fields
    6. If `get_meeting_transcript` returns empty or errors, verify your Granola plan supports transcript access (Business or Enterprise tier required)
  </how-to-verify>
  <resume-signal>Paste the output of `list_meetings` (or a summary showing the fields returned) and confirm `get_meeting_transcript` works. If transcript access is blocked by plan tier, say "blocked - free tier" so we can pause the phase.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Extend /debrief skill with Granola fetch, company-notes append, and enhanced filler tracking</name>
  <read_first>
    - .claude/skills/debrief/SKILL.md (current skill definition - the file being modified)
    - coaching/anti-pattern-tracker.md (anti-pattern schema to understand update format)
    - framework/templates/recruiter-session.md (session template format)
    - .planning/phases/01-granola-mcp-integration-call-analysis-coaching-loop/01-RESEARCH.md (Granola API details, transcript format, code examples)
  </read_first>
  <files>.claude/skills/debrief/SKILL.md</files>
  <action>
Update `.claude/skills/debrief/SKILL.md` with three additions. The file uses markdown skill format with YAML frontmatter.

**Addition 1: Update frontmatter allowed-tools**

Add `Write(data/company-notes/**)` to the `allowed-tools` field so the skill can append to company notes. The current value is:
```
allowed-tools: Read(*), Glob(*), Grep(*), Write(coaching/**), Edit(coaching/**)
```
Change to:
```
allowed-tools: Read(*), Glob(*), Grep(*), Write(coaching/**), Edit(coaching/**), Write(data/company-notes/**)
```

**Addition 2: Insert new "Step 0: Acquire Transcript" before existing Step 1**

Insert a new section between the `## Instructions` header and `### Step 1: Load Context`. The new step handles two input modes per D-01/D-02:

```markdown
### Step 0: Acquire Transcript

Check if a transcript has been pasted into the conversation context.

**If transcript is present:** Skip to Step 1 (existing flow, unchanged).

**If no transcript is present:** Fetch from Granola MCP.

1. Call `list_meetings` to retrieve recent meetings (last 14 days)
2. Present a numbered picker table to the user:

   | # | Title | Date | Duration |
   |---|-------|------|----------|
   | 1 | [meeting title] | [date] | [duration] |
   | 2 | [meeting title] | [date] | [duration] |
   | ... | | | |

3. Ask: "Which call would you like to debrief? (Enter number)"
4. After user selects, call `get_meeting_transcript` with the selected meeting's ID
5. Parse the transcript segments into Q&A pairs:
   - **Merge consecutive segments** from the same speaker source (Granola splits on pauses, not speaker turns)
   - `source: "speaker"` = interviewer/recruiter (the other person)
   - `source: "microphone"` = Nick (the candidate)
   - Pair each merged "speaker" block with the following merged "microphone" block to form one Q&A pair
   - If the candidate speaks without a preceding question (e.g., opening intro), mark the question as "(unprompted / opening)"
   - For panel interviews (multiple remote speakers), note that all remote participants appear as "speaker" - flag this in the debrief output
6. Extract the company name from the meeting title for use in Step 1 (loading the correct cheat sheet and company notes)

If `list_meetings` returns no results or `get_meeting_transcript` fails, tell the user: "Could not fetch transcript from Granola. Please paste the transcript directly, or verify your Granola plan supports transcript access."

**Deriving CV path from Granola meeting:** When using Granola (no CV path argument), attempt to match the meeting title to an existing company slug in `output/`. If a match is found, use the most recent CV file in that directory. If no match, ask the user for the CV path or proceed without a cheat sheet (see "No cheat sheet fallback" below).
```

**Addition 3: Insert "Step 6.5: Update Company Notes" after existing Step 6**

After the existing Step 6 (Log Session), add a new step:

```markdown
### Step 6.5: Update Company Notes

After logging the session, append structured call intel to the company notes file.

1. Determine the company slug from the CV path or Granola meeting title
2. Read `data/company-notes/<slug>.md` (create if it doesn't exist, with header `# <Company Name> - Notes`)
3. **Use Write (read-then-write), never Edit** - this file may have long rows
4. Prepend a new section at the top (below the header), formatted as:

```
## YYYY-MM-DD | [Meeting type] with [interviewer name if known]

**Source:** Granola transcript / Voice simulation / Pasted transcript
**Duration:** [if known]
**Overall rating:** [confidence rating]/5

### Key Intel
- [Bullet points of factual information learned: team size, tech stack, timeline, budget, pain points, org structure]

### Questions They Asked
- [List of interviewer questions - useful for future prep]

### Signals
- [Positive signals: enthusiasm, "we need someone like you", timeline urgency]
- [Negative signals: hesitation, "we have other candidates", scope concerns]

### Follow-up Items
- [Action items: send materials, schedule next round, research topic mentioned]
```

5. If no company slug can be determined, skip this step and note: "Could not determine company - skipping company notes update. Run manually if needed."
```

**Addition 4: Add "No cheat sheet fallback" note to Step 3**

In the existing Step 3 (Analyze Each Answer), add this note after the Step 3C (Comparison to Coached Answer) section:

```markdown
**No cheat sheet fallback:** If no cheat sheet or coached answers exist for this company (common for first-time calls or companies without prep), skip Step 3C (coached-answer comparison) but run all other analysis: filler counts, anti-pattern detection, Q&A grading, trust impact assessment. Include in the report: "No cheat sheet found for [company]. Coached-answer comparison skipped. Consider running /prep-interview [company] to generate one before the next call."
```

**Addition 5: Enhance filler word specificity in Step 3D**

In the existing Step 3D (Anti-Pattern Check), add after the anti-pattern list:

```markdown
**Filler word tracking (per D-06):** For the "Filler hedging words" anti-pattern, count specific occurrences of each tracked filler in the candidate's speech:
- "really" (word boundary match)
- "kind of" / "kinda"
- "definitely"
- "to be honest with you"
- "absolutely"
- "pretty" (when used as hedge before adjective: "pretty good", "pretty much", etc.)

Report individual counts in the debrief and include them when updating the anti-pattern tracker Update Log. Example format for the Update Log entry:
`| 2026-04-08 | Company - Role (Granola/voice sim) | Filler: really x3, kind of x1; [other patterns] | [notes] |`
```

**Do NOT change** any other part of SKILL.md. The existing Steps 1-6 remain exactly as they are, with only the insertions described above.
  </action>
  <verify>
    <automated>grep -c "list_meetings" .claude/skills/debrief/SKILL.md && grep -c "company-notes" .claude/skills/debrief/SKILL.md && grep -c "Step 0" .claude/skills/debrief/SKILL.md && grep -c "Step 6.5" .claude/skills/debrief/SKILL.md && grep "allowed-tools" .claude/skills/debrief/SKILL.md | grep -c "company-notes"</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md frontmatter `allowed-tools` contains `Write(data/company-notes/**)`
    - SKILL.md contains `### Step 0: Acquire Transcript` section
    - SKILL.md contains `list_meetings` (at least 2 occurrences - in picker and fallback)
    - SKILL.md contains `get_meeting_transcript` (at least 1 occurrence)
    - SKILL.md contains `### Step 6.5: Update Company Notes` section
    - SKILL.md contains `data/company-notes/<slug>.md` reference
    - SKILL.md contains the six tracked filler words: "really", "kind of", "definitely", "to be honest with you", "absolutely", "pretty"
    - SKILL.md contains "No cheat sheet fallback" text
    - Existing Steps 1-6 are unchanged (Step 1 still starts with "Load Context", Step 6 still starts with "Log Session")
    - The `## Prerequisites` section still mentions pasted transcript as option 2 (but no longer the only option)
  </acceptance_criteria>
  <done>SKILL.md contains Granola MCP fetch (Step 0), company-notes append (Step 6.5), enhanced filler tracking, and no-cheat-sheet fallback. Existing debrief flow (Steps 1-6) is preserved intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude Code -> Granola MCP | Transcript data crosses from external service into local analysis |
| Skill -> data/company-notes/ | Analysis output written to user's data files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | Granola MCP OAuth | accept | OAuth 2.1 with PKCE is industry standard; tokens managed by official MCP server, not stored locally by us |
| T-01-02 | Tampering | Anti-pattern tracker writes | mitigate | Use Write (read-then-write full file) per CLAUDE.md convention; never Edit on files with long table rows |
| T-01-03 | Denial of Service | Granola rate limits | accept | Interactive use is well within 100 req/min MCP limit; no mitigation needed for manual debrief flow |
</threat_model>

<verification>
1. Invoke `/debrief` without pasting a transcript - verify Granola picker appears with meeting list
2. Select a meeting - verify transcript is fetched and Q&A pairs are parsed
3. Verify full debrief analysis runs (filler counts, anti-patterns, coached comparison if cheat sheet exists)
4. Verify company notes file is created/appended after session logging
5. Verify anti-pattern tracker Update Log has a new entry with filler counts
6. Invoke `/debrief` WITH a pasted transcript - verify existing flow still works unchanged
</verification>

<success_criteria>
- Granola MCP is configured and returning meeting data
- /debrief shows Granola picker when no transcript is pasted
- Full debrief analysis runs on Granola-fetched transcripts
- Company notes auto-populated after debrief
- Existing paste-transcript workflow unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/01-granola-mcp-integration-call-analysis-coaching-loop/01-01-SUMMARY.md`
</output>
